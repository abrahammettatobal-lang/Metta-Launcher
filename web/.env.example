# URL pública del sitio (metadata Open Graph, canonical, APIs).
NEXT_PUBLIC_SITE_URL=https://metta-launcher.railway.app

# Railway inyecta PORT automáticamente; en local usa 3000 por defecto.
PORT=3000
