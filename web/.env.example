# URL pública del sitio (Railway custom domain o *.up.railway.app)
# Usada en metadata Open Graph, canonical y futuras APIs.
NEXT_PUBLIC_SITE_URL=https://tu-dominio.up.railway.app

# Railway inyecta PORT automáticamente; en local usa 3000 por defecto.
PORT=3000
